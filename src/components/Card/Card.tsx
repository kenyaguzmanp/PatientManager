import React, { FC } from 'react';
import { View, Text } from 'react-native';

interface CardProps {}

const Card: FC<CardProps> = () => {
  return (
    <View>
      <Text>Card</Text>
    </View>
  );
};

export default Card;
